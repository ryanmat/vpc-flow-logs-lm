# Description: Environment variable template for local development.
# Description: Copy to .env and fill in your values. Never commit the .env file.

# Required: Your LogicMonitor portal name (e.g., "acmecorp")
LM_COMPANY_NAME=yourportal

# LM portal domain (default: logicmonitor.com)
LM_COMPANY_DOMAIN=logicmonitor.com

# Phase 1 (Ingest API) credentials
LM_ACCESS_ID=your_access_id
LM_ACCESS_KEY=your_access_key

# Phase 2 (Webhook) credentials
LM_BEARER_TOKEN=your_bearer_token

# Webhook source name (default: GCP-VPC-FlowLogs)
WEBHOOK_SOURCE_NAME=GCP-VPC-FlowLogs

# Set to "true" for Phase 2 webhook path, "false" for Phase 1 ingest API
USE_WEBHOOK=false
